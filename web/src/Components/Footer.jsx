export default () => {
    return (
        <footer className="mt-64 bg-black text-sm text-white text-center py-4 block">
            Fact-Shield {new Date().getFullYear()} &copy; all right reserved. created by Naveed ahmad (BCS hazara university sub-campus battagram).
        </footer>
    )
}